# Cursor research (P0)

- Local IDE version: `3.14.7`
- `agent` CLI: **unavailable** on this machine PATH
- Docs describe Cursor CLI non-interactive / structured output — treat startTask as unknown until CLI is installed and sampled
- Foreground IDE approval lifecycle: **unsupported** as confirmed API; UI must show UNKNOWN or inferred with confidence

Refs: https://docs.cursor.com/en/cli/overview
