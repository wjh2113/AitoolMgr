# CLAUDE.md — AitoolMgr

Follow `AGENTS.md` and `docs/prompts/00-preamble.md`.

Before coding: read `docs/research/evidence-matrix.md` and the current stage prompt in `docs/prompts/`.

Never invent Codex / Cursor / VS Code / OpenClaw event names. Prefer local `--help`, official docs, and fixtures under `spikes/` / `tests/fixtures/`.
