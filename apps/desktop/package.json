{
  "name": "@toolmgr/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "electron": "electron electron/main.mjs",
    "pack:win": "npm run build && npx --yes electron-builder@25 --win --config electron-builder.yml",
    "pack:mac": "npm run build && npx --yes electron-builder@25 --mac --config electron-builder.yml",
    "pack:all": "npm run build && npx --yes electron-builder@25 --win --mac --config electron-builder.yml"
  },
  "dependencies": {
    "@toolmgr/core": "*",
    "@xyflow/react": "^12.4.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.0"
  }
}
